<div class="content-header"><h2 class="content-title">Manage Permissions: {{ $role->role_name }}</h2></div><div class="table-wrapper" style="max-width: 600px;"><form method="POST" action="{{ route('roles.updatePermissions', $role) }}" class="ajax-form" style="padding: 1.5rem;">@csrf<div class="form-group"><p style="margin-bottom: 1rem; font-size: 0.9rem; color: #666;">Select permissions for this role:</p>@foreach($permissions as $perm)<label style="display: block; margin-bottom: 0.5rem;"><input type="checkbox" name="permission_ids[]" value="{{ $perm->permission_id }}" {{ $role->permissions->contains($perm->permission_id) ? 'checked' : '' }}> {{ $perm->permission_name }} <span style="color: #999; font-size: 0.85rem;">({{ $perm->description }})</span></label>@endforeach</div><div class="form-actions"><button type="submit" class="btn">Save Permissions</button><a href="{{ route('roles.index') }}" class="btn btn-secondary ajax-link">Cancel</a></div></form></div>
