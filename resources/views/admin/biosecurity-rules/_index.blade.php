<div class="content-header"><h2 class="content-title">Biosecurity Rules</h2><a href="{{ route('biosecurity-rules.create') }}" class="btn ajax-link">+ Add</a></div><div class="table-wrapper"><table><thead><tr><th>Origin</th><th>Destination</th><th>Area Type</th><th>Min Down (hrs)</th><th>Max Down (hrs)</th><th>Level</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($biosecurity_rules as $rule)<tr><td>{{ $rule->originFarm->farm_name ?? '-' }}</td><td>{{ $rule->destinationFarm->farm_name ?? '-' }}</td><td>{{ $rule->area_type ?? '-' }}</td><td>{{ $rule->minimum_downtime ?? '-' }}</td><td>{{ $rule->maximum_downtime ?? '-' }}</td><td>{{ $rule->access_level }}</td><td><span style="background: {{ $rule->is_active ? '#28a745' : '#dc3545' }}; color: white; padding: 0.25rem 0.5rem; border-radius: 3px; font-size: 12px;">{{ $rule->is_active ? 'Active' : 'Inactive' }}</span></td><td><a href="{{ route('biosecurity-rules.edit', $rule) }}" class="btn ajax-link" style="background: #17a2b8; font-size: 12px;">Edit</a><form method="POST" action="{{ route('biosecurity-rules.destroy', $rule) }}" class="ajax-form" style="display: inline;" onsubmit="return confirm('Sure?');">@csrf
            @method('DELETE')<button type="submit" class="btn btn-danger" style="font-size: 12px;">Delete</button></form></td></tr>@empty<tr><td colspan="8" style="text-align: center; padding: 2rem;">No records.</td></tr>@endforelse</tbody></table></div>
