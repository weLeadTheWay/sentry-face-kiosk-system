<p><a href="{{ route('biosecurity-rules.index') }}" class="ajax-link">&larr; Biosecurity Rules</a></p>
<div class="content-header"><h2 class="content-title">Downtime Stationary</h2><a href="{{ route('downtime-stationary.create') }}" class="btn ajax-link">+ Add</a></div>
<div class="table-wrapper"><table><thead><tr><th>Assigned Farm</th><th>Min Down (hrs)</th><th>Max Down (hrs)</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($downtime_stationary_rules as $rule)<tr><td>{{ $rule->assignedFarm->farm_name ?? '-' }}</td><td>{{ $rule->minimum_downtime_hours ?? '-' }}</td><td>{{ $rule->max_downtime_hours ?? '-' }}</td><td><span style="background: {{ $rule->is_active ? '#28a745' : '#dc3545' }}; color: white; padding: 0.25rem 0.5rem; border-radius: 3px; font-size: 12px;">{{ $rule->is_active ? 'Active' : 'Inactive' }}</span></td><td><a href="{{ route('downtime-stationary.edit', $rule) }}" class="btn ajax-link" style="background: #17a2b8; font-size: 12px;">Edit</a><form method="POST" action="{{ route('downtime-stationary.destroy', $rule) }}" class="ajax-form" style="display: inline;" onsubmit="return confirm('Sure?');">@csrf
            @method('DELETE')<button type="submit" class="btn btn-danger" style="font-size: 12px;">Delete</button></form></td></tr>@empty<tr><td colspan="5" style="text-align: center; padding: 2rem;">No records.</td></tr>@endforelse</tbody></table></div>
